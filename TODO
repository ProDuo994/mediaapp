/*BACKEND:
Detect when a client sends a message then send that message to all other accounts in the server:
Requirements:
Multi-Account Login (DONE)

FRONTEND:
Wait for new messages then publish new messages to chat:/\
*/
